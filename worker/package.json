{
  "name": "eal-robotik-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "login": "wrangler login",
    "secret": "wrangler secret put",
    "check": "wrangler deploy --dry-run --outdir .tmp-check"
  },
  "devDependencies": {
    "wrangler": "^4.122.0"
  }
}
